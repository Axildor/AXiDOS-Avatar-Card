{
  "name": "axidos-avatar-card",
  "version": "5.3.0",
  "description": "AXiDOS Avatar Card for Home Assistant — modular source, bundled to a single HACS-distributable file",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node build.mjs && node --check axidos-card.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "license": "MIT"
}